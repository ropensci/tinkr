TEXT $\theta^{0}$ TEXT2 $p(\theta^{0} | X) > 0$. TEXT3 $\theta^*$ TEXT4 $T_i$. TEXT5 $\theta^* \sim N(\theta^{i}, \sigma^2)$. TEXT6.

TEXT7 $\theta^*$ TEXT8.

$$r = \frac{p(\theta^* | X) / T_i(\theta^* | \theta^{i})}{p(\theta^i | X) / T_i(\theta^{i} | \theta^{*})},$$
TEXT9 $\theta^{i+1} = \theta^*$ TEXT10 $\min(r, 1)$. TEXT11 $\theta^{i+1} = \theta^{i}.$ TEXT12 $r$ TEXT13.
